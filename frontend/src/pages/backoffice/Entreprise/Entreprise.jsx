import React, { useEffect } from "react";

export default function Entreprise() {
  useEffect(() => {
    // INFO DB
    // LISTE OFFRE => cf tableau offre
  }, []);

  return <div>Entreprise</div>;
}
